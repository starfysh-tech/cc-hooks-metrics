[project]
name = "hooks-report"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["textual>=8.0,<9.0", "rich>=14.0"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
